#include <QtCore>

/* An example of calculating a hash value. Not needed in the exercise.
uint qHash( const QPoint& p )
{
    return qHash(p.x()) ^ qHash(p.y());
}
*/

int main(int argc, char** argv)
{
    Q_UNUSED(argc);
    Q_UNUSED(argv);

    return 0;
}
